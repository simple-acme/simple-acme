# Codex Prompt Notes

## Step 4 clarification (stub connectors)

The previously stubbed device types referenced in `setup/Device-Schemas.ps1` are superseded by the native connector implementation pass defined in `codex-prompt-native-integration.md` Step 2. For those device types, use full connector implementations rather than stub-only handling.
